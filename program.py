from tkinter import *

window = Tk() # creating window

window.title("Calculator") # naming window
window.geometry("500x500") # set window's resolution

window.update_idletasks()
window.resizable(False, False)
window.mainloop()